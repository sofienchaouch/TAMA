package net.eniso.main.service;

import net.vpc.upa.config.*;
import java.util.logging.Logger;

/**
 * Application UPA interceptor for Main module.
 *
 * @author sofiene
 */
@Callback
public class MyAppMainInterceptor {
    private static final Logger log = Logger.getLogger(MyAppMainInterceptor.class.getName());
}